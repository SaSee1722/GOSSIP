import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.sasee1722.gossip',
    appName: 'GOSSIP',
    webDir: 'out',
    server: {
        androidScheme: 'https'
    }
};

export default config;
